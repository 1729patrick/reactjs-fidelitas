import React from 'react';
import ResponsiveDrawer from '../../components/Drawer';

const ReservesView = () => {
  return (
    <ResponsiveDrawer>
      <div>Reserves</div>
    </ResponsiveDrawer>
  );
};

export default ReservesView;
